{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "login-webview"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default"
  ]
}
